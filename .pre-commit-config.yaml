repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Ruff version.
    rev: v0.14.14
    hooks:
      # Run the linter, including isort-style import ordering and docstrings.
      - id: ruff-check
        args: ["--fix", "--select", "I,D"]
      # Run the formatter.
      - id: ruff-format
        types_or: [python, pyi]
  - repo: https://github.com/psf/black
    rev: 24.10.0
    hooks:
      - id: black
